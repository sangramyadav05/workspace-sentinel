"""
MEMORY RULES (HARD RULES — DO NOT CHANGE LIGHTLY)

1. The AI is NOT allowed to decide what is remembered.
2. The AI is NOT allowed to write to long-term memory by itself.
3. Every memory write MUST require explicit user approval.
4. Memory is stored locally only (no cloud, no sync).
5. The user can inspect all stored memory at any time.
6. The user can delete any memory at any time.
7. No secrets, API keys, or credentials are ever stored.
8. Memory must never be written silently.
9. Short-term memory is cleared when the program exits.
10. Long-term memory persists only approved data.
"""
